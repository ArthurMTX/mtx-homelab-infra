- name: Deploy homelab services (Homepage)
  hosts: homelab
  become: true
  gather_facts: false

  pre_tasks:
    - name: Ensure python3-pip is installed
      ansible.builtin.yum:
        name: python3-pip
        state: present

  roles:
    - role: homepage
      tags: ["homepage"]
